# bench/

Benchmark code will live here: ring microbenchmarks, timing utilities,
histograms, load generators, multi-run drivers, and report generation. The goal
is to keep measurement code separate from the matching engine itself.
